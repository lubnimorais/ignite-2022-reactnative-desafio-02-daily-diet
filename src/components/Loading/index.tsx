import { LoadingContainer, LoadingIndicator } from './styles';

export const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingIndicator size="large" />
    </LoadingContainer>
  );
};